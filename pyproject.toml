[project]
name = "src"
version = "1.0.0"
description = "A website for hosting instructions on how to install HGSS and Platinum ACE exploits"
dependencies = [
    "flask",
    "psycopg2-binary"
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"
